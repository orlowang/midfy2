schema {
  query: Query
}

type Menu {
  id: String
  name: String
  keyprop: String
  url: String
  child: [Menu]
}

interface Node {
  id: ID!
}

type Query {
  node(id: ID!): Node
  viewer: Viewer
}

type Viewer {
  menu: [Menu]
}
